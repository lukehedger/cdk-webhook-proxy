{
  "name": "cdk-webhook-proxy",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "yarn workspaces run build",
    "synth": "yarn workspaces run synth"
  },
  "workspaces": [
    "aws-deployment-pipeline",
    "aws-github-webhook",
    "aws-infrastructure-cloud"
  ]
}
